---
- name: rhc RHEL system role
  hosts: all
  become: true
  vars:
    rhc_auth:
      activation_keys:
        keys:
          - "{{ activation_key }}"
        rhc_organization: "{{ organization_id }}"
  tasks:
  roles:
    - redhat.rhel_system_roles.rhc
